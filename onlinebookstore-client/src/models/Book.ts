//kenzie whitman
export interface Book {
    bookID: number;  
    title: string;
    author: string;
    publisher: string;
    isbn: string;
    category: string;    
    classification: string;
    pageCount: number;
    price: number;
}